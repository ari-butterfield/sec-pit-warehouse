version: 2

models:
  - name: int_facts__versioned
    description: One row per filing, business, tag, date, time range.
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - accession_number
            - central_index_key
            - tag
            - end_date
            - count_of_quarters
